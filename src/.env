Current Features

WebRTC video + audio call

Local and remote video streams.

Mute/unmute and camera toggle.

QR code + link signaling

Generates compressed SDP.

Shares via JSONBlob and QR code.

QR scanning via html5-qrcode.

Chat over data channel

Sends and receives text.

Shows “peer is typing” indicator.

Theme toggle (light/dark).

Call history (via db.js).

Connection stats (bitrate).

End call cleanup (media tracks, data channel).

Recent calls list.

Responsive layout (video + chat + side panels).